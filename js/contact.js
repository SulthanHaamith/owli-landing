/**
 * CONTACT.JS
 * PURPOSE: Handles the contact/booking form:
 *          - Validates form fields (name, phone, email, service, message)
 *          - Builds a WhatsApp URL with pre-filled message
 *          - Opens WhatsApp with the form data
 * HOW IT WORKS: Will be built in a later phase.
 *   - Validates required fields and formats (email regex, Indian phone)
 *   - On submit, constructs a wa.me link with form data
 *   - Falls back to mailto: link if WhatsApp isn't available
 */
