/**
 * I18N.JS (Internationalization)
 * PURPOSE: Handles bilingual support (English / Tamil).
 *          Contains all translated strings and the logic
 *          to swap text when the user toggles language.
 * HOW IT WORKS: Will be built in a later phase.
 *   - A translations object holds all strings in 'en' and 'ta' keys
 *   - Elements with data-i18n="key" attributes get their text swapped
 *   - Selected language is saved in localStorage
 */
